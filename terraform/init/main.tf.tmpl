{{- if eq .FolderName "0-terraform-statestore" }}
terraform {}
{{- else }}
terraform {
  backend "gcs" {}
  required_providers {
    local = {
      source = "hashicorp/local"
    }
    random = {
      source = "hashicorp/random"
    }
  }
}
{{- end }}

provider "google" {}

